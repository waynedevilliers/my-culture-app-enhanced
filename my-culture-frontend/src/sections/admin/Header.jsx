import Navigation from '../../components/admin/Navigation.jsx';

const Header = () => {
  return (
    <header className="fixed flex justify-center w-screen px-4 top-0 z-50">
      <Navigation />
    </header>
  );
};

export default Header;
